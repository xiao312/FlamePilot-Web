# Backend server port
PORT=50002

# Frontend port (Vite)
VITE_PORT=50001

# Gemini CLI path (optional)
# If gemini is not in your PATH, specify the full path here
# Example: GEMINI_PATH=/home/user/miniconda3/envs/xx/bin/flame-pilot
# GEMINI_PATH=flame-pilot

# Shell sandbox root (optional, strongly recommended)
# When set, the shell and file APIs are restricted to this root directory.
SHELL_ROOT=/home/user/safe-root

# Database file path (optional)
# DATABASE_PATH=./data/database.sqlite

# JWT Secret (required for production)
# Generate a secure random string for production use
# openssl rand -base64 32 (run this in your terminal)
JWT_SECRET=your-secret-key-here

# Node environment
NODE_ENV=development

# Photon charging configuration
# Set PHOTON_DEV_MODE=1 for development mode (uses DEV_ACCESS_KEY, CLIENT_NAME, SKU_ID)
# Set PHOTON_DEV_MODE=0 for production mode (uses user cookies)
PHOTON_DEV_MODE=1

# In dev mode, set PHOTON_MOCK=1 to skip real API calls (just log charges)
PHOTON_MOCK=1

# Dev mode credentials (your own Bohrium account for testing)
DEV_ACCESS_KEY=your-dev-access-key
CLIENT_NAME=deepflame-cfd-agent-uuid1760867389
SKU_ID=10051

# Production SKU ID (your app's Photon SKU)
PHOTON_SKU_ID=10051

# Photon billing whitelist (comma-separated accessKeys). If set, these keys are not charged.
# Example: PHOTON_WHITELIST_ACCESS_KEYS=ak1,ak2
PHOTON_WHITELIST_ACCESS_KEYS=
