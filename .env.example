# Backend server port
PORT=4008

# Frontend port (Vite)
VITE_PORT=4009

# Gemini CLI path (optional)
# If gemini is not in your PATH, specify the full path here
# Example: GEMINI_PATH=/home/user/miniconda3/envs/xx/bin/flame-pilot
# GEMINI_PATH=flame-pilot

# Database file path (optional)
# DATABASE_PATH=./data/database.sqlite

# JWT Secret (required for production)
# Generate a secure random string for production use
# openssl rand -base64 32 (run this in your terminal)
JWT_SECRET=your-secret-key-here

# Node environment
NODE_ENV=development